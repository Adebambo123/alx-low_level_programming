#include <stdio.h>
/**
 * main - entry
 * Return: (0)
*/
int main(void)
{
	char ch;

	for (ch = "a"; ch <= "z"; ch++)
	{
		if (ch != "e" && ch != "q")
			putchar(la);
	}

	putchat("\n");

	return (0);
}
